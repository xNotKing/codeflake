---
import Layout from '../../layouts/Layout.astro';
import Nav from '../../components/Nav.astro';
---
<Layout title="Codeflake | PY: Print">
    <Nav>
		<a href="/">Home</a>
		<a href="/cs">CS</a>
	</Nav>
    <main>
        <h1>Python | Print</h1>
        <p>Probably the first thing that you want to create with Python is a simple program that outputs a given text.To perform this we must use the function "print". Each programming languague has its own syntax and to print some text into our screen we must follow this syntax: </p>
        <code>
            print("Hello world")
        </code>
        <p>The code above will output Hello world. We can also use emojis 😱, symbols, and mathematical operations on our prints.</p>
        <div class="next" onclick="location.href = '/python/math'" style="background-color: wheat; padding: 10px; border-radius: 7px; width: auto; text-align: center;">Next lesson</div>
    </main>
</Layout>
<style>
    main {
        width: 80%;
        max-width: 520px;
        padding: 0 16px;
    }
    main p {
        margin-top: 20px;
        margin-bottom: 20px;
    }
    code {
		width: 100%;
		padding: 16px;
		color: #fff;
		background-color: #3f5c6f;
		border-radius: 7px;
		font-family: system-ui, sans-serif;
	}
    a {
        text-decoration: none;
        color: #000;
    }
    img {
        object-fit: cover;
        width: 100%;
        height: 200px;
    }
    @media (max-width: 450px) {
        img {
            object-fit: contain;
        }
    }
</style>